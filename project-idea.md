I want to publish a GitHub pages site that hosts some photos of me and my girlfriend.

The idea is that i will print each photo on a physical card, attach to it a NFC device (NTag215) that codifies the URL of the corresponding photo on the GitHub pages site.

The GitHub page should be a beautiful and user-friendly gallery where each photo can be accessed via the NFC-enabled cards.

On the website each photo i would like:
- To be displayed in high resolution.
- To have a short description i've written.
- To have the position and the date where i've taken the photo.

This last two information should be derived from the file's metadata (EXIF data).

I need all the website scheleton, and a way (a tool) to add a photo.

The style should be nothing too fancy, but with a good design.